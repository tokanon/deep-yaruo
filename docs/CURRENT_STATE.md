# Current project state

Updated: 2026-09-07

This is the compact operational handoff for resuming work after context compaction. It records approval state, not merely implementation state. Detailed history remains in the linked phase documents and roadmap.

## Established direction

- P3 through P6 are completed rejected experiments. P3 raw-raster C1 may remain a historical DeepAA comparison baseline. P4 through P6 used pixel masks or tone channels without first indexing fill as explicit surfaces, so their algorithms, metrics, parameter findings, and failure diagnoses must not constrain the accepted-v2 reverse-channel design or be cited as evidence against it. Keep their files only as an audit trail of the rejected approach. Any exact-width helper still used by P6R-G1 is justified only by its later independent P6R tests, not by P4. This distinction was explicitly corrected by the user on 2026-09-07.
- Improvements that pass their stage-specific safety and human gates are intended to accumulate in the normal generator. Comparative UI is a temporary evaluation mechanism, not the final destination of accepted work. On 2026-09-06 the user explicitly corrected the prior assumption that P6R-4L, P6R-4I, and the useful provisional part of P6R-4T3 should remain disconnected indefinitely. The cumulative recipe was subsequently approved and implemented as P6R-G1; T3 remains a provisional stage rather than a formal quality acceptance.
- P6R-1 and P6R-2 passed their implementation gates. P6R-2S passed as the correction-pair storage foundation. P6R-3 conditionally passed as a candidate-surface generator. See [P6R-1](AA_FILL_SURFACES_P6R1.md), [P6R-2](SURFACE_METRICS_P6R2.md), [P6R-2S](CORRECTION_PAIRS_P6R2S.md), and [P6R-3](SURFACE_PROPOSALS_P6R3.md).
- P6R-4 has an implemented storage, correspondence, and rule-evaluation foundation, but its strict correction-pair quality gate is unevaluated. Its transparent surface-rule components are used inside P6R-G1 with stage safety fallback; the unevaluated correction-pair gate does not block accepted-v2 reverse-channel training. See [P6R-4](SURFACE_DECISIONS_P6R4.md).
- For user evaluation, the established low-burden interaction is to choose one generated candidate or mark all unusable. Manual AA correction is optional and is reserved for examples that need a true correction pair; it is not required for every sample.
- Do not describe the current exact-width placement as evidence recovered from P4. P4's `ch2` fill experiment is design-reference-invalid. The helper code happens to share implementation ancestry, but its current acceptance comes only from later P6R row-width and line-preservation tests under explicit surface IDs.
- The P6R-4 surface-fill comparison design was explicitly approved on 2026-09-02. It uses one shared DeepAA/Beam baseline and four blinded candidates: no fill, plus the same coarse Lab surface layer with decision thresholds 0.60, 0.45, and 0.30. The three fill candidates share P4's variable-width placement guards: 2px boundary margin, 85% cell coverage, 65% safe coverage, 4% maximum structure coverage, and runs of at least three characters.
- That first recipe was superseded after three person selections. It selected surfaces but often placed no fill: the standard candidate selected 5-15 surfaces yet produced zero replacements on person-02 and person-03. The user approved `surface-fill-v2` on 2026-09-02: all fill candidates use surface score 0.30, crop-relative grayscale P10/P90 darkness bands, a one-pixel boundary margin, and conservative/standard/expanded tone-adaptive placement tables. Minimum runs remain three.
- The approved first pass uses the existing eight person and seven background source images. Additional images are not requested until that pilot is reviewed. Line-art profiles are outside this fill comparison because rule-v1 has no tone and abstains.

## Implemented but not approved for the intended evaluation

- The current four user-facing draft variants, `baseline-v1`, `detail-v1`, `simplify-v1`, and `weak-lines-v1`, vary line extraction and preprocessing settings.
- They are not an approved substitute for evaluating P6R-4 surface-constrained fill output.
- Collection with these four variants is stopped unless the user explicitly approves a separate line-setting preference study.
- Any already-saved preference record may remain as an implementation artifact, but it must not be counted as P6R-4 fill-quality evidence.

## Approved implementation

- Replace the active line-setting comparison with the approved surface-fill comparison while retaining the ordinary single-result generator.
- Use `lab-l4-a4-b4` for all fill candidates. In the superseding v2 recipe, keep `minimum_fill_score=0.30` common and vary only the approved crop-relative tone-adaptive placement tables. Convert selected surface darkness into the local glyph-tone map; do not use the rejected global `ch2` decision.
- Save new comparisons separately as `draft-preferences/v2` with comparison kind, generator version, recipe version, surface configuration, decision mask and decisions, replacement diagnostics, artifacts, and selection. Keep v1 records unchanged and exclude them from P6R-4 evidence.
- The selection-only pilot is a human-preference gate, not a substitute for paired surface IoU, overfill, or omission metrics. The correction-pair P6R-4 quality gate remains unevaluated until suitable optional correction pairs exist.

- The approved comparison and v2 storage are implemented. The active UI uses `POST /api/surface-fill-comparison` and `POST /api/surface-fill-preferences`; the old v1 API and records remain intact but are not used by the active comparison.
- The implementation gate passed on 2026-09-02: 134 tests passed and 1 skipped, the frontend production build passed, and a real `person-01-source.png` smoke generated four candidates at 48 columns / 32 maximum rows. Its replacement counts were 0 / 0 / 20 / 22 for no-fill / conservative / standard / expanded, with 0.000571 main-line loss for the two changed candidates. This smoke is not a human quality result.
- Removal of the completed weak-v1/v2 pseudo-line blind-review feature was explicitly approved and completed on 2026-09-02. Its active React workspace, `/api/weak-review/*`, storage implementation, dedicated report commands, and tests are gone. The generated weak-v1/v2 data, rejected-experiment documents, and shared generation helpers needed to reproduce P3-P6 evidence remain. After removal, 129 tests passed with 1 skipped and the production frontend build passed.
- The three saved `surface-fill-v1` person selections remain immutable but are excluded from the restarted v2 pilot.
- The first `surface-fill-v2` implementation made standard placement nonzero on all three person sources and preserved exact row widths. Conservative/standard/expanded replaced 633/712/797 cells on person-01, 131/172/221 on person-02, and 224/307/370 on person-03. The expanded candidate failed the approved 2% main-line-loss cap on person-01 (2.6789%) and person-02 (2.6572%); standard remained below the cap on all three. The v2 implementation gate is therefore not passed and collection remains stopped.
- The user approved the corrective v2 checkpoint on 2026-09-02: retain expanded fill/safe coverage at 50/30, 60/40, and 70/50, but cap expanded dark/middle/light structure allowance at 8/6/4%, equal to standard. No other v2 behavior changes.
- The corrected v2 implementation gate passed. Conservative/standard/expanded replaced 633/712/733 cells on person-01, 131/172/182 on person-02, and 224/307/354 on person-03. Main-line loss was at most 1.6883%, standard was nonzero on every source, fill increased monotonically, and every row width matched. After implementation, 129 tests passed with 1 skipped and the production frontend build passed.
- The user completed the three-source v2 review: person-01 and person-02 selected standard, person-03 selected conservative, and none were marked unusable. The user judged the fill amount acceptable but the three filled candidates visually similar.
- The user approved the next order on 2026-09-02: freeze `surface-fill-v2` and finish the remaining 12 selections first; after all 15 are saved, replace the near-duplicate conservative/standard/expanded amount presentation with a weak/standard/strong fill-amount slider; then evaluate a separate adjacent-surface texture stage. That stage must deterministically graph-color touching selected surfaces with up to four style IDs and use empirically supported punctuation motifs of similar average density. It must not claim skin/clothing/background semantics without labeled segmentation.
- The approved future adjacency-texture gate is: differentiate at least 80% of touching selected-surface pairs where compatible motifs exist; mean darkness error at most 0.03 versus the current fill; exact row widths; main-line loss at most 2%; deterministic output; and per-surface fallback to the current glyph when no compatible motif exists. If the overall gate fails, do not enable the stage by default. This stage is not to be implemented before the 15-record v2 pilot is complete.
- The 15-record `surface-fill-v2` pilot was completed on 2026-09-03. Selections were standard 5, conservative 2, expanded 3, and all-unusable 5. All-unusable was 2/8 for person sources and 3/7 for background sources. The user reported that dark-heavy inputs often lost even their lines or received little to no fill.
- Inspection separated two failure classes. Person-04, person-07, and background-07 had only 0, 5, and 11 replaced source cells in the standard recipe; their processed line images discarded much of the low-contrast internal structure and their dark border-connected regions were poorly represented by the selected-surface mask. Background-06 reached 234 replaced cells but was still unusable, so fill texture/organization is also independently inadequate. Background-02 was pale rather than dark and still lost low-contrast structure, showing that absolute darkness alone is not the cause.
- The user approved P6R-4L on 2026-09-03 before the slider-only cleanup and P6R-4T. Its four blinded variants are current v2-standard, local-contrast line recovery only, border-connected dark-surface recovery only, and both recoveries. Only the five all-unusable v2 sources are reviewed, selections are stored separately in `draft-preferences/v3`, and v2 remains immutable. The human gate is a recovery variant selected on at least 3/5 sources; otherwise it is not connected to normal generation.
- P6R-4L is implemented. Local line recovery preserves the current line mask, adds only source-gradient-supported connected strokes, and caps line pixels at 1.5x. Border recovery uses contact-ratio-proportional penalty with a 2/3 safety floor, retains the large-area penalty, permits 0% structure coverage for newly rescued surfaces, and falls back within the candidate if final main-line loss exceeds 2%. The real five-source implementation gate passed for all 20 candidates: current-line retention 1.0, source-supported additions 1.0, line multiplier at most 1.5, exact row widths, and main-line loss at most 2%. The human gate passed 5/5 on 2026-09-04: line-only was selected twice, surface-only once, and combined twice; current was selected zero times and none was marked all-unusable. See [P6R-4L](SURFACE_RECOVERY_P6R4L.md).
- Final verification on 2026-09-03 passed: 134 tests passed with 1 skipped, the production frontend build passed, `git diff --check` reported no whitespace errors, and regenerating all five failed v2 records again returned `mechanical_gate_passed: true`.
- The user judged all five recovery comparisons better than before, then clarified that the remaining issue is not chiefly darker contrast: visible source structure is still omitted and substantial regions become pure white. Representative source/processed/rendered inspection confirmed that person clothing shading and background wall/building information are already absent in the processed representation, before glyph selection. Merely darkening output glyphs or starting P6R-4T would not restore that missing information.
- The explicitly approved P6R-4I diagnostic audit completed on 2026-09-04. Across the five selected v3 results, 11,259 of 17,295 source-informative proxy cells were blank (65.10%). Primary causes were surface proposal/decision omission 61.23%, selected-surface placement omission 17.84%, preprocessing structure loss 17.69%, and glyph conversion loss 3.23%. These percentages apply only to the five deliberately difficult failures and are diagnostic proxies, not general quality metrics. See [P6R-4I](INFORMATION_LOSS_P6R4I.md).
- P6R-4I verification passed with 137 tests passed and 1 skipped, no `git diff --check` errors, validated v3 artifact hashes, and deterministic reproduction of the five-case aggregate.
- P6R-4I recovery was explicitly approved and implemented as four blinded variants over the same five v3 records: selected-v3 baseline, omitted-tone-surface recovery, selected-surface placement recovery, and both. It removes 4-connected components below three half-width cells, changes only blank/structure-free source-supported intervals, preserves Saitamaar row width, and caps recovered cells at 40% of baseline blank-information cells. All 20 real candidates passed safety gates. Aggregate blank-information reduction was 25.21% for omitted surfaces, 3.50% for placement only, and 27.14% combined, so the 25% implementation gate passed. Person-04 combined improved only 9.64%, so improvement is not claimed for every case.
- Final P6R-4I recovery verification passed with 142 tests passed and 1 skipped after adding the v3-setting mismatch regression test; the production frontend build passed, and `git diff --check` reported no errors.
- The P6R-4I human gate passed on 2026-09-04. All five v4 records selected a recovery candidate: omitted-surface recovery for all three backgrounds and combined recovery for both persons. Baseline, placement-only, and all-unusable were selected zero times. This is a relative preference result on five deliberately difficult cases and does not by itself authorize normal-generation integration.
- The user clarified on 2026-09-04 that the existing sample set should remain unchanged. Although difficult and not intrinsically AA-friendly, that difficulty makes generator differences easier to evaluate. The proposed replacement-sample workflow was rejected; treat the current images as an intentionally hard stress-evaluation set rather than claiming they represent ordinary inputs.
- After the v4 collection completed, the user explicitly approved removing the three completed experiment controls from the normal AA-generation UI. `塗り4案比較`, `低コントラスト回収4案`, `情報欠落回収4案`, their candidate cards, and frontend preference-save flow are removed. `AAを1案生成`, the editor, correction-pair saving, exports, immutable v2-v4 data, and the comparison/reproduction APIs remain. The production frontend build and the full 142-test suite passed after removal.

## Current action

The accepted-v2 0500 LS model is now the provisional default for color-image cumulative generation. It receives P6R-4L recovered lines and P6R-3/4 selected Lab surfaces as separate membership, ID-boundary, and source-darkness-tone channels. Its 16-phase dense scanner evaluates learned character/start heads at every x; exact-width DP uses start cost at selected boundaries and non-start cost inside each glyph advance, with no target start count. Do not run the old P6R output fill/information/T3 stages again on LS output. Model/inference failure, width failure, or a non-empty input row becoming all whitespace falls back for the whole generation to P6R-G1. Line-art profiles remain on P6R-G1, and cumulative OFF remains the old DeepAA baseline. Correction pairs pin LS ONNX/vocabulary hashes plus LS/fallback provenance.

The fixed 15-case real-image gate passed on 2026-09-07: exact width 15/15, deterministic text/PNG/pipeline 15/15, no non-empty input row decoded entirely as whitespace, no target start count, and fallback 0/15. The user waived a separate A/B gate for this provisional integration after the synthetic LS improvement. This is not a claim of visual superiority on real images. CPU generation was roughly 10-20 seconds per fixed case on the development machine and remains a performance limitation. See `docs/DEEPAA_SURFACE_V0.md` and `.tmp/training-runs/deepaa-surface-real-v0/report.json`.

Final verification for provisional integration passed: 183 normal-environment tests with 2 optional skips, 7/7 Torch-specific tests, frontend production build, ONNX export parity error `2.288818359375e-05`, and `git diff --check`. Runtime ONNX SHA-256 is `3E4F702A2E106654A4ADF2C4875EB09A65259F58BEC4F44184570D5D661BB304`; runtime vocabulary SHA-256 is `71548DED84946EC2C6CEB92BEA3BDD3F50185D0A9ABB134D4EA7C50A0DBBD857`; the fixed real-image report SHA-256 is `03BEB6D881464037D1AAF4AE3F63613A79C925B8C53CFCEE99726EA1CDE011DE`.

P6R-G1 cumulative normal generation remains the frozen fallback. Its color recipe is DeepAA; P6R-4L local line recovery; P6R-3/4 candidate surfaces and decisions; `surface-fill-standard-v2`; P6R-4L border dark-surface recovery; combined P6R-4I omitted-surface and placement recovery; provisional P6R-4T3 proximity texture. Completed T3 A/B controls are removed while reproduction APIs and immutable v5/v6 evidence remain.

The 15-case stress evaluation passed on 2026-09-07: all 15 preserved exact row widths and passed pipeline safety; maximum texture density error was 0.02962963, maximum texture main-line loss was 0.008305, and two complete runs produced identical text and PNG for 15/15 cases. Information and provisional texture stages applied on 15/15. Surface recovery selected combined-v3 on 12 and lines-v3 on 3. Texture changed-character coverage ranged from 0.00455235 to 0.99282748, so the very low person-05 coverage remains explicit evidence of the limited accepted-v1 pattern vocabulary, not a claim that T3 is generally adequate. Unit tests and frontend build also passed. See `.tmp/training-runs/cumulative-p6rg1/report.json` for the local detailed report.

Do not ask the user to redo any v2-v6 records or replace the current stress samples. The accepted-v2 collection design was approved and implemented. Its candidate selection is completely independent of all 317 old review records: it does not use their presentation history, decisions, category corrections, or near-duplicate exclusions. The same AA may be shown again and receives a new v2 decision. Keep accepted-v1 and the old review database immutable only for reproduction.

The active review UI uses `reviews-v2.sqlite3`, counts progress by accepted works, requires only accept/hold/reject plus a concrete category for accepted works, and makes the 0-5 score, tags, and comments optional; it does not require manual AA editing. It persists deterministic 50-item batches and uses dimensions only for ranking. The optional Starlette `TestClient` smoke is omitted because the development-only `httpx2` package is absent; do not install it solely for this workflow.

The first v2 collection reached 250 accepted on 2026-09-07 after 661 reviews (250 accept, 411 reject, 0 hold), covering 214 source MLTs. The immutable `yaruyomi-accepted-v2-0250` snapshot at `accepted-v2/checkpoints/0250` is accepted as a collection-continuation checkpoint, not a training-ready split. Hash verification, whitespace duplicates, mirrored near-duplicates, and cross-split source overlap passed. One cross-MLT near-duplicate pair was found across train and validation: entry 471124 (quality 4) and entry 1105359 (quality 3), both `character`, length ratio 0.977972 and 4-gram overlap 0.898699.

The user clarified that both near variants were correctly accepted on their individual quality: a copied or modified work can be reviewed before a later, better original appears. Do not rewrite either decision merely to obtain a round corpus count. Candidate selection suppresses only exact and whitespace-only duplicates; modified works remain reviewable. At snapshot time, cross-MLT near and mirrored variants must be grouped into the same split. Before training, remove a variant only if its redundancy demonstrably harms learning or evaluation; the final trainable count need not equal the collection milestone and removed items do not require replacement.

The immutable 500-work snapshot and its two audits are complete at `accepted-v2/checkpoints/0500`. It contains 500 accepted works from 425 source MLTs after 1,445 reviews (945 reject, 0 hold). Hashes, exact/whitespace duplicates, cross-split near and mirrored variants, and source-MLT isolation passed. The known two-MLT related pair is retained and assigned wholly to test. All 12 categories occur in train, validation, and test. It has 1,256 unique non-whitespace characters over 1,300,258 occurrences; the old 899-character DeepAA table covers 99.719671% of occurrences. Repeated fill runs occur in 438/500 works, multi-line fill surfaces in 411/500, and the empirical supported motif catalog grew to 24 patterns. Compared with accepted-v1 this adds 350 source MLTs, 618 unique characters, 9.6 percentage points of fill-work coverage, 10.2 points of multi-line-surface coverage, and 19 supported motifs. `training.audit_review_checkpoint` marks it ready for reverse-channel training design, not corpus saturation.

The approved 0500 reverse-channel extraction and audit are complete. `accepted-v2-reverse-channels-v1` keeps the original text, positioned render, glyph starts, natural Saitamaar advances, row widths, separate line/fill glyph masks, integer surface IDs, surface tone attributes, and run metadata. Candidate A is the P6R-1 single-character extractor; candidate B is its strict superset with 1-4 character periodic motifs and no glyph-width cap. Both reconstructed 500/500 target renders exactly and had zero surface integrity failures. Two complete builds had identical payload SHA-256 `e594f72f12d4e28907e44808aa83ea88321144ddf4dcf075fbedf658389ff3cc`. B covered 476 works and 524,041 fill glyphs versus A's 438 and 337,312. The 24 highest-disagreement previews, two per category, were visually audited; B predominantly moved hatching/tone regions rather than principal contours or dialogue, so B was selected for the model checkpoint. Its full catalog has 400 observed motifs but only 30 with the frozen cross-work support flag. The support-handling question recorded by that audit was resolved by the approved checkpoint below. See `docs/REVERSE_CHANNELS_V1.md` and the local `accepted-v2/derived/0500/reverse-channels-v1/audit.json`.

That model checkpoint was explicitly approved on 2026-09-07. Compare `L`, which receives only the B-derived line mask, with `LS`, which keeps a separate line CNN and surface CNN and fuses their features late. The surface branch receives three value-independent channels: surface membership, boundaries between/outside indexed surfaces, and normalized surface tone; raw integer surface IDs, motif names, and motif frequencies are never model inputs. Train-observed characters all remain separate classes without a frequency cutoff. Validation/test characters absent from train are reported as unseen rather than converted silently to an unknown class. Both variants use the same split, vocabulary, samples, optimization, and exact-width decoder conditions and predict character class, character start, and an auxiliary line/fill role. Report character/start/render metrics overall and for line, fill, supported-motif fill, and low-support fill. Use validation to fix the checkpoint and decoder once, then evaluate test once. Stop LS if it does not improve fill, materially degrades line/no-fill, loses exact row width, or cannot accept the corresponding real-image line/surface contract. Save the experiment under `.tmp/training-runs/deepaa-surface-v0`; do not connect either model to normal generation from synthetic results alone.

That 0500 L/LS synthetic comparison is complete and passed all predeclared synthetic stop checks. Both best checkpoints were epoch 3. On the once-opened 55-work test, LS versus L was 88.13% versus 75.69% overall character accuracy, 94.36% versus 90.14% line, 67.94% versus 28.82% fill, 69.46% versus 29.50% supported fill, and 17.75% versus 6.27% low-support fill. Fill-region render mismatch fell from 6.91% to 5.89%, line-region mismatch from 3.22% to 1.70%, and target-ink omission from 55.24% to 31.12%; both variants matched 2,391/2,391 row widths. All 35 test occurrences unseen in train were reported separately and counted wrong. The 1-9-train-occurrence bucket remained poor and declined from 7.69% to 5.98%, so 0500 is not corpus saturation. The synthetic decoder uses the held-out true start count and therefore is not an end-to-end real-image decoder; start precision/recall were evaluated separately. Normal generation is unchanged. The next material action is a design checkpoint for an unseen-real-image LS versus P6R-G1 comparison, not automatic integration or another synthetic retune. See `docs/DEEPAA_SURFACE_V0.md` and `.tmp/training-runs/deepaa-surface-v0/comparison-test.json`.

Verification for this phase passed with 179 tests and 2 optional skips in the normal environment, 6/6 Torch-specific tests in the training environment, all 500 B fill/support consistency checks, compilation, and `git diff --check`. A training-environment full-suite attempt hit only a Windows temporary-directory ACL failure while creating pytest fixtures; it is not recorded as a code failure because the normal suite and the separately required Torch suite both passed.

On 2026-09-07 the user conditionally authorized provisional integration because the L/LS synthetic result improved materially and waived a separate human A/B gate if the end-to-end decoder had no blocking problem. That condition is now satisfied by the dense learned-start decoder and fixed 15-image gate recorded above. The user refers to the broader accepted-v2 feedback/retraining loop as reinforcement; keep the implementation distinction explicit: the completed 0500 LS update is supervised self-training, while pairwise reward-model or DPO-style reinforcement remains later work after more accepted-v2 data and real-image feedback.

The 500 collection exposed a sampler flaw: all 22 indexed background MLTs and all 7 nature MLTs had reached the old three-presentation cap, leaving zero eligible candidates, so the allocator silently filled the total with other categories. The snapshot therefore has background 20/40, nature 13/24, and text-or-joke 23/24, though each still has train/validation/test coverage. The user approved fixing this for the next 750 milestone without changing the frozen 500 snapshot. Selection policy v6 stops only when total accepted is at least 750 and every scaled category floor is met, weights each 50-item batch by corrected-category deficit divided by observed direct acceptance yield, retains the three-per-MLT limit normally, and expands only an exhausted under-target category in balanced per-source rounds. A copied real-DB smoke batch produced 7 background and 3 nature candidates instead of zero. The 750 snapshot command accepts a total above 750 after all floors are met. Verification passed with 177 tests and 1 skipped, the frontend production build passed, and `git diff --check` reported no whitespace errors. After implementing this policy, return to the 500-work reverse-channel design checkpoint before asking the user to collect more.

On 2026-09-07 the user corrected a roadmap drift: `accepted-v2` is not merely a texture-vocabulary collection followed by mandatory hand-authored source-image correction pairs. Its primary role is the finished-AA corpus for the DeepAA-style self-supervised path. After freezing it, derive separate line and fill/surface channels from each accepted AA, preserve their shared geometry and the original AA as the target, and train/evaluate the next generator from those synthetic pairs. Whether line/fill inversion uses separate CNNs or a multi-head model remains a design decision after corpus audit. Do not merge line and fill back into the rejected weak-v1/v2 single proxy image, and do not reuse P6's failed low-frequency `ch1` unchanged.

Real-image correction pairs remain an implemented optional validation/calibration facility, not the next mandatory corpus and not the primary training source. First compare the reverse-channel model with P6R-G1 on held-out accepted works and real stress/unseen images using mechanical checks and low-burden A/B evaluation. Create only a small targeted correction set if the synthetic-to-real gap cannot be diagnosed or measured adequately without exact correspondence. Do not impose the former person-10/background-10 correction quota or 20/50/100/200 correction-pair learning curve as a prerequisite for the reverse-channel path.

The user approved an adaptive scale for the replacement corpus on 2026-09-07. Keep `accepted-v1` immutable for reproduction, but do not let any old review record affect v2 candidate selection or decisions. Create separately versioned 250-work `accepted-v2` snapshots. Treat 500 accepted as the first full training checkpoint, not as a freeze target; plan for 750-1,000 accepted or more and continue by 250 while coverage or held-out/real-image performance is still improving. Freeze only after successive increments show saturation and the important categories have leakage-free MLT coverage in train, validation, and test. The exact numeric saturation thresholds, sampling, category floors, v2-internal duplicate handling, and user workload remain the next design checkpoint. Character-reference corruption is not a reason to discard the old files: the frozen snapshot has four accepted records marked `character_references`, and the exported 317 texts contain no remaining literal character-reference tokens. The reasons to rebuild are scale, category balance, pattern coverage, and a stronger finished-AA target corpus for DeepAA-style self-supervision.

The user approved the remaining high-level order on 2026-09-07: build the adaptive accepted-v2 corpus; train a DeepAA-style generator from separately reverse-generated line and indexed fill/surface channels; improve line continuity, character placement, deformation, and global two-dimensional structure; iterate corpus/model generation with held-out real-image comparisons; finish editor, OCR, compatibility, performance, and Windows distribution; then pass unseen-image acceptance tests for v1.0. Do not insert a return to P4-P6, a mandatory hand-correction corpus, or an unrelated large generative-model detour into this critical path.

Future approved product direction: manga-panel inputs need an OCR text channel separate from line and fill. Small dialogue, annotations, and small sound effects should retain recognized text plus position, orientation, and confidence for width-aware placement in the AA output; large decorative onomatopoeia should normally remain visual line/fill structure. This is roadmap-only for now and follows P6R-G1 and `accepted-v2`. OCR choice, the size boundary in target AA coordinates, vertical text and ruby handling, fallback, and correction UI remain unapproved implementation details. See `docs/ROADMAP.md`, section 7.1.

Future requested product direction: for person/character generation, an optional checkbox should remove non-character background before the generator receives its input. With the checkbox off, current generation must remain unchanged. This is roadmap-only and not the current action. Segmentation method, mask preview/editing, multiple-character handling, failure fallback, saved provenance, performance, and acceptance criteria remain unapproved implementation details and require a design checkpoint before implementation. See `docs/ROADMAP.md`, section 7.2.

For the pending P6R-4T design, the user explicitly rejected restricting texture motifs to punctuation or to an invented fixed pixel width. Motif discovery may begin from accepted-v1 periodic patterns, but it must use actual Saitamaar advances, may include other empirically observed fill characters, and must rerun the complete accepted-v1 aggregation whenever the extraction rules or pattern catalog expand. A separate fill CNN is an open architectural question, not an approved implementation; do not train one from rule-generated pseudo-labels and claim an independent quality improvement.

P6R-4T0 motif extraction was approved and implemented. Its first stress-set simulation was invalid because assigning v4 fill-mask pixels outside Lab proposals to synthetic connected components fragmented background-06 into 2,373 apparent surfaces and background-07 into 6,406. The user explicitly approved evaluating only stable Lab proposal IDs; unowned recovery cells abstain and retain their current glyph. The corrected audit passed: accepted-v1 100 yielded 96 candidates and six supported motifs (`.`, `.:`, `:`, `;`, `:i`, `二`); 13/15 stress images had 97 compatible stable-surface adjacency pairs; the selected four-motif palette (`;`, `二`, `:i`, `:`) separated 97/97 pairs with maximum density error 0.02777778. The five targeted tests and full suite (147 passed, 1 skipped) passed, and `git diff --check` reported no errors. See [P6R-4T](SURFACE_TEXTURE_P6R4T.md).

The P6R-4T body design was explicitly approved and its mechanical evaluation completed on 2026-09-05. It replaced only existing repeated fill-run intervals owned by stable Lab IDs, using natural Saitamaar advances, at least two motif periods, space-only padding, per-interval rendered density error at most 0.03, 65% owner coverage, and at most 4% extracted-line coverage. Across the audited 97 stable adjacency pairs, only 44 pairs (45.36%) had different motifs actually placed on both sides, below the required 78. All 15 cases preserved exact row widths and deterministic output; maximum density error was 0.02880658 and maximum main-line loss was 0.000449. The failure is exact-width motif/run compatibility, not synthetic surface fragmentation. Per the approved stop rule, no A/B UI, v5 preference collection, or normal-generation connection was added, and the human gate remains unevaluated. Final verification passed with 150 tests passed and 1 skipped, and no `git diff --check` errors. Any broader pattern catalog or changed placement rule requires a new checkpoint and full accepted-v1 100 plus stress-set 15 reaggregation. See [P6R-4T](SURFACE_TEXTURE_P6R4T.md).

P6R-4T1 was explicitly approved on 2026-09-05. Keep stable-surface ownership and every existing safety gate, but exclude any main or edge-adjustment glyph whose individual Saitamaar advance exceeds the 8px half-width reference; this removes the 16px `二` without capping the total width of a multi-character motif. Reaggregate accepted-v1 all 100 to choose up to four main motifs and collect empirically observed characters immediately at the left and right edges of supported fill runs. Actual placement may use at least two main-motif periods plus at most one observed adjustment character on each corresponding edge. Then rerun all 15 stress cases. Do not add human-review UI, v5 collection, or normal-generation integration unless at least 78 audited adjacency pairs receive different actually rendered main motifs and all prior safety gates pass.

P6R-4T1 completed and failed its mechanical gate. Full reaggregation yielded 62 candidate and five supported motifs; the palette became (`;`, `:i`, `:`, `.`), with theoretical separation on 93/97 pairs. Edge adjustment increased replaced intervals from 1,276 to 1,618 but did not increase the 96/150 surfaces receiving a main motif. Actual separation fell from 44/97 to 38/97, below the required 78. Eligibility diagnosis showed all 150 target surfaces overlapped a fill run, 103 had at least one character cell owned at least 65% by one stable surface, 102 also passed the 4% line-coverage cap, and 96 passed final width/density fitting. Thus edge compensation addressed only the final six-surface fitting loss; the dominant loss is 47 surfaces whose stable region does not own enough of any indivisible glyph cell. Removing the 16px `二` also reduced density-compatible style choices. Maximum density error 0.02962963, maximum main-line loss 0.00101, row widths, and determinism passed. Final verification passed with 151 tests passed and 1 skipped, and no `git diff --check` errors. Per the stop rule, no human review, v5 storage, UI, or normal-generation integration was added. Do not lower the 65% owner threshold, split glyph cells, or broaden the palette without a new design checkpoint.

The user rejected the adjacency/ownership premise behind those P6R-4T/T1 gate counts on 2026-09-05. Two ordinary fill surfaces are separated by a line, and that separator belongs to neither fill. The implementation instead built edges only where two positive Lab owner labels touched directly and then treated fill-run cells crossing a proposal boundary as an ownership failure. Consequently the 97-pair denominator does not represent the intended line-separated fill adjacency, and 44/97 and 38/97 are invalid as P6R-4T quality-gate judgments. Keep them only as diagnostics of the rejected direct-contact graph. A corrected design must leave separator line cells unowned, define an edge between stable fill interiors across the same separator line, and count only fill interiors that are actually textureable. Do not implement or collect human preferences until that replacement graph and denominator receive explicit approval.

P6R-4T2 was explicitly approved on 2026-09-05. Classify baseline AA characters as existing repeated-fill cells, non-space non-fill separator cells, or blank. Assign only existing fill cells to the stable Lab label with maximum overlap; remove the 65% threshold, while cells with no stable overlap remain unowned. Separator cells remain unowned and immutable. Build graph edges only between different stable fill owners on opposite sides of a contiguous separator run in one row, or above and below a separator cell over its actual Saitamaar advance interval; direct Lab-label contact and blank-only gaps are not edges. Keep the T1 palette, edge adjustment, exact-width, density, line-preservation, and deterministic constraints. Define raw line-separated pairs first, then evaluation-eligible pairs only where the boundary-adjacent fill intervals can take distinct motifs. At least 80% of raw pairs must be eligible and at least 80% of eligible pairs must actually render different motifs, with at least five pairs across three images. Run all 15 without user work; only after a mechanical pass may the fixed five-case A/B review and v5 storage/UI be added. Normal generation remains unchanged.

P6R-4T2 passed its mechanical gate on 2026-09-05. The corrected denominator contains seven raw line-separated stable-fill pairs across four images; all seven were eligible and all seven received different actually rendered main motifs. Maximum density error was 0.02962963, maximum main-line loss was 0.010757, all 15 cases preserved exact row widths, and two complete generations were deterministic. The five-case v5 review completed with the texture candidate selected twice, no preference three times, baseline zero, and both unusable zero. The user confirmed a modest effect but rejected T2 as the intended design: fill surfaces need different motifs based on spatial proximity even when a small unfilled exterior or blank gap lies between them, whereas T2 relates only surfaces sharing its narrowly recognized separator. Keep v5 immutable as T2 evidence, do not treat its mechanical pass or 2/5 preference as approval, and do not connect T2 to ordinary generation. A replacement proximity-weighted assignment requires a new explicit checkpoint and a new recipe/storage version.

P6R-4T3 was explicitly approved on 2026-09-05. Retain stable fill surfaces, the frozen T1 motif catalog, exact Saitamaar widths, density error at most 0.03, main-line loss at most 2%, immutable separator/non-fill cells, and determinism. Do not require direct contact or a recognized shared separator. Compute minimum surface distance in Saitamaar-normalized coordinates (8px horizontal, 18px vertical), weight every textureable surface pair by `1 / (1 + distance)^2`, and assign motifs to minimize nearby same-motif weight so distant surfaces may reuse a motif. The mechanical denominator is each textureable surface's nearest compatible surface, deduplicated; require at least five pairs across three images and at least 80% actually rendered with different motifs. Rerun all 15 but not accepted-v1 100 because the motif extraction/catalog is unchanged. On a mechanical pass only, replace the completed v5 control with blinded T2-versus-T3 comparison on the same five images, save separately to v6, and require T3 preference on at least 3/5 before proposing normal-generation integration. Preserve immutable v5 data and reproduction APIs.

P6R-4T3 passed its mechanical gate on 2026-09-05. Across all 15 stress cases, the nearest-compatible denominator was 92 deduplicated pairs across 14 images and 89/92 pairs (96.74%) actually rendered with different motifs. Maximum density error was 0.02962963, maximum main-line loss was 0.010757, all row widths matched, and two complete generations were deterministic. The frozen T1 catalog was unchanged, so accepted-v1 100 was not rerun. The active temporary UI now compares T2 and T3 blindly on the same fixed five images and saves separately to immutable `draft-preferences/v6`; v5 storage and reproduction APIs remain. Human evaluation is 0/5, and T3 must be selected on at least 3/5 before any normal-generation integration is proposed. Verification passed with 161 tests and 1 skipped plus the frontend production build.

The five v6 selections completed on 2026-09-05: T3 was selected for background-02, background-04, and person-06; T2 was selected for background-06 and person-04; no neutral or unusable result was recorded. Although this numerically reaches the predeclared 3/5 threshold, the user observed in the live person-04 comparison that visually distinct black background and black clothing still used the same motif and judged the visible change small. Inspection of that exact open comparison and saved record confirmed the dominant gate defect: person-04 had 2,692 fill characters and 2,685 owned fill characters, but T3 changed intervals containing only 160 characters (5.94%). One motif is assigned per stable owner; intervals incompatible with that motif's density or width silently retain their original fill. `applied_surface_count=6` still counted all six owners because one changed interval per owner was enough. The 89/92 metric covers only nearest compatible owner pairs and has no changed-character coverage requirement; it therefore substantially overstates visible application. A stable owner may also contain multiple visually separate fill regions. Treat the human gate as unresolved rather than accepted; normal integration remains blocked. A redesign must receive explicit approval before implementation or more user evaluation.

The user explicitly rejected manually re-entering stored comparison settings on 2026-09-05. For the v6 comparison only, the button must resolve the unique saved v2 or selected replacement-v4 baseline by source SHA-256, generate with that record's options and crop regardless of current controls, and copy those resolved values back into the UI. Missing or ambiguous baseline records still fail closed. Normal generation and the immutable v5 reproduction path remain unchanged.

The user explicitly chose to stop immediate P6R-4T repair on 2026-09-05. With only 100 accepted reference AAs, the frozen catalog contains four usable main motifs and too few density-compatible alternatives; person-04 demonstrates that exact width and density preservation then leave most fill unchanged. Preserve T0-T3 and v5-v6 as rejected or diagnostic experiment evidence. On 2026-09-06 the user nevertheless requested that T3's bounded current benefit be included provisionally in the cumulative generator instead of withholding all texture separation until the catalog grows. This provisional availability is not a P6R-4T quality acceptance. After `accepted-v2` or another pattern source expands the vocabulary, rerun the complete active accepted corpus and all 15 stress cases, add actual changed-character coverage to the gate, and obtain a new design checkpoint before replacing the provisional texture stage.

## Resume references

Read these only as needed for the active decision:

- `AGENTS.md`
- `docs/ROADMAP.md`, sections 5.1, 5.6, 6 and the current critical path
- `docs/CURATED_CORPUS.md`
- `docs/DATA_REVIEW_GUIDE.md`
- `docs/YARUYOMI_IMPORT.md`
- `training/README.md`
- `docs/DEEPAA_SURFACE_V0.md`
- `training/deepaa_surface_v0.py`
- `training/train_deepaa_surface_v0.py`
- `training/evaluate_deepaa_surface_v0.py`
- `training/review_corpus.py`
- `backend/review.py`
